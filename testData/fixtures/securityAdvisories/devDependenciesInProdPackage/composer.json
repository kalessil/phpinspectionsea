{
  "name":        "kalessil/whatever",
  "description": "",
  "license":     "",
  <warning descr="Please add roave/security-advisories:dev-master into require-dev as a firewall for vulnerable components.">"require"</warning>: {
    <warning descr="Dev-packages have no security guaranties, invoke the package via require-dev instead.">"phpunit/PHPUnit"</warning>: "*",
    <warning descr="Dev-packages have no security guaranties, invoke the package via require-dev instead.">"roave/security-advisories"</warning>: "dev-master"
  }
}