sudo: false
language: java
jdk:
  - oraclejdk8

notifications:
  email: false

script: "./travis.sh"
cache:
  directories:
    - .cache
before_script:
 - chmod +x travis.sh

env:
  - PHPSTORM_ENV=2017.1

