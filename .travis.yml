sudo:     false
language: java
jdk:
  - oraclejdk8

notifications:
  email:  false

script: ".travis/build.sh"

cache:
  directories:
    - .travis/.cache

before_script:
 - chmod +x .travis/build.sh

matrix:
  include:
    - env: IDE_ID=eap
    - env: IDE_ID=2017.1


